# GitHub repository settings

This file is for maintainers. It does not change product behavior.

These settings are configured in the GitHub UI (`Settings`), not in this repository's source. Do not invent metrics or topics that misrepresent the project.

## Suggested description

```text
See how AI agents see your website. SEO + AI-agent readiness inspector — Chrome Extension, GitHub Action, and CLI.
```

Keep it under GitHub's description length. Do not claim official endorsement by any AI provider.

## Suggested website

Leave empty until there is a real public site or Chrome Web Store listing.

## Suggested topics

```text
seo
ai-agents
web-crawling
web-development
developer-tools
chrome-extension
github-actions
typescript
seo-tools
structured-data
robots-txt
sitemap
llms-txt
```

`nextjs` is accurate for `apps/web` but optional if you want the topic list to stay product-facing.

## Social preview

Upload `docs/assets/github-social-preview.png` (1280×640) once a real image exists. See [docs/assets/README.md](assets/README.md).

## Releases

There is a GitHub release (`v0.1.0`). Keep the README release badge only while that remains true.

Do not add an npm version badge until a package published from **this** repository exists. The npm name `agentlens` is not automatically this project.

## After this pass

1. Set the description and topics above
2. Record `docs/assets/agentlens-demo.gif` from the real extension
3. Add a social preview image
4. Publish a short launch note once the GIF is in the README
